#include <stdio.h>
#include "serrure.h"
/*
struct Tag tagAdmin={0x00, 0x00, 0x00, 0x00};
struct Tag tagUser={0x00, 0x00, 0x00, 0x00};
enum etat etat = attente;

bool reconnait_badge_admin(struct Tag tag)
{
    if (tag == {0x00, 0x00, 0x00, 0x00}) {tagAdmin = tag;}
    if (tag_are_equals(tagAdmin, tag)) {etat = menuAdmin; return true;}
    return false;
}

bool reconnait_badge_util(struct Tag tag)
{
    if (tag == {0x00, 0x00, 0x00, 0x00}) {tagAdmin = tag;}
    if (tag_are_equals(tagAdmin, tag)) {etat = menuAdmin; return true;}
    return false;
}
*/